/*
 * StartPage Messages
 *
 * This contains all the text for the StartPage component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.StartPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StartPage component!',
  },
});
