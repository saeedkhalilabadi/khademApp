/*
 * BrancheMenu Messages
 *
 * This contains all the text for the BrancheMenu container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BrancheMenu';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BrancheMenu container!',
  },
});
